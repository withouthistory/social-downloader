[Unit]
Description=Social Downloader API
After=network.target

[Service]
User=root
WorkingDirectory=/root/social-downloader
ExecStart=/root/social-downloader/venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
